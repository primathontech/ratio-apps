// Wizzy uses a simple Store ID + Store Secret (no OAuth flow),
// so this file is intentionally minimal — kept for structural parity with admin-google.
// If a Wizzy OAuth flow is added in future, wire it here.
export {};
