<!-- TEMPLATE: document THIS vendor's search API contract here. -->

# `__Slug__` Storefront Search API — vendor contract

This file is a placeholder. When scaffolding a real storefront SDK from this
template, replace it with the extracted contract for **this** vendor's search
API: the base URL, auth headers, endpoint paths/params, and response shapes the
`src/client.ts` wrapper depends on.

The finished reference SDK this template was distilled from keeps the
equivalent document as `docs/<slug>-search-api-contract.md`.
