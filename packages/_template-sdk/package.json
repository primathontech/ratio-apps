{
  "name": "@ratio-app/__slug__-sdk",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build && vite build --config vite.loader.config.ts && vite build --config vite.results.config.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "e2e": "playwright test",
    "size": "size-limit",
    "lint": "biome check src"
  },
  "dependencies": { "@ratio-app/shared": "workspace:*", "lit": "^3.2.0" },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "@size-limit/preset-small-lib": "^11.0.0",
    "@types/node": "^22.0.0",
    "happy-dom": "^15.0.0",
    "size-limit": "^11.0.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^2.1.0"
  }
}
